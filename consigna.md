Utilizando .NET Core genere un proyecto ASP vacío, e implemente los endpoints de los métodos HTPP (GET y POST).
    a. Investigue qué tipo de ruteo se utilizó.
    b. El GET a la raíz debe mostrar un formulario que al ejecutarlo envíe dicho formulario al POST.
    c. El POST en el servidor debe validar que los datos no vengan vacíos, respondiendo con un mensaje indicando si todo salió bien o si falta algún dato.
    d. Genere una presentación reducida explicando los conceptos vistos.
        i. Tipo de Ruteo.
        ii. Tecnologías involucradas.
        iii. Métodos HTTP.